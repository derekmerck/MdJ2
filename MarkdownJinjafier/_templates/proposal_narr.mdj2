# {{ title }}
{% for a in authors %}
* {{ a }}
{%- endfor %}

{{ blocks.background | render_blocks(level=2) }}

{{ blocks.approach   | block_boilerplate('deidentified', 'data-collection', deidentified) | render_blocks(level=2) }}

{{ blocks['preliminary-results'] | render_blocks(level=3) }}

{{ blocks['expected-results']  | render_blocks(level=2) }}

Risk 
----------------

{{ boilerplate(risk, risk is defined) }}


Bibliography
----------------